from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Creating an ML pipeline, connecting it to a fast api endpoint, dockerizing the endpoint and deploying it on google cloud',
    author='Vinay Kamath',
    license='',
)
